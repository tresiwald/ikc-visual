**IKC-Visual (PAWI)**

Prototyp of a graph visualization library.
Mainly used in the existing ikc-core prototype.

To start developing:

npm install
npm update
npm install npm -g

run webpack target:
build

and finally

start
